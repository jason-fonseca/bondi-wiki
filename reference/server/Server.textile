h2(#summary). Summary

h2(#syntax). Syntax

h2(#parameters). Parameters

h2(#description). Description

h2(#properties). Properties

- Timeout := Setting for timeout in Milliseconds

h2(#methods). Methods

- Sleep(ms) := Sleep for ms Milliseconds
- Include()
- Require() := Like server include - but ensures the file being pulled in only enters scope once for the current execution
- Import(module name) :=

Takes the object referenced at the end of the included file and returns it, this makes for an interesting module system

```javascript
var moduleName = Server.Import("modulename.esp");

moduleName.test();

in module file:

new (function() {
  this.test = function() { return true; };
})();
```

=:

- Execute() := executes script

- Transfer()
- ExecuteAsync() :=you can pass an object as third arg to Server.ExecuteAsync and it will stream it via msg-pack to Request.Form in the child
- ExecuteAsyncNoReturn() :=

=:

h2(#examples). Examples

h2(#seealso). See also