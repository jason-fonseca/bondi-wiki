h2(#summary). Summary

Constructable object available in [[Bondi|Bondi Reference]]

h2(#syntax). Syntax

@var imap = new ImapClient([username [, password]]);@

h2(#parameters). Parameters

- username := The username to login with to imap server.
- password := The password for username to login with.

h2(#description). Description

h2(#properties). Properties

- ConnectTimeout :=

=:
- Debug
- Password
- ReceiveTimeout
- ResponseBody
- ResponseObject
- ResponseText
- ResponseXML
- SendTimeout
- Status
- StatusText
- Username

h2(#methods). Methods

- Close() :=

=:
- GetAllResponseHeaders()
- GetResponseHeader()
- Send()
- SetRequestHeader()

h2(#examples). Examples

h2(#seealso). See also