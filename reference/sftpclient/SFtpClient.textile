h2(#summary). Summary

Constructable object available in [[Bondi|Bondi Reference]]

h2(#syntax). Syntax

@var sftpClient = new SFtpClient([host [, username [, password]]]);@

h2(#parameters). Parameters

- host := The sftp hostname or ip address to connect to.
- username := The username to login with on the host.
- password := The password for the username to login with.

h2(#description). Description

h2(#properties). Properties

- ConnectTimeout :=

=:
- HomeDirectory
- Host
- Password
- ReceiveTimeout
- SendTimeout
- Username

h2(#methods). Methods

- Close() :=

=:
- Delete()
- Get()
- Move()
- Permissions()
- Put()

h2(#examples). Examples

h2(#seealso). See also